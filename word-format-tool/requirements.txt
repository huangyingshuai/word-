# 强制使用预编译包，不进行源码编译（核心！）
--only-binary :all:

#  Streamlit：用最稳定的通用版本，不锁范围
streamlit

# Word文档处理核心
python-docx>=0.8.11

# Pillow：直接用最新稳定版，它会自动适配Python 3.12，有预编译包
pillow

# 其他必要依赖
numpy>=1.26.0
